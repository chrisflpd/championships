{
  "name": "championships-test",
  "private": true,
  "description": "checks for the scheduler, the page and the workbook it exports",
  "scripts": {
    "test": "node page.js && node robust.js",
    "page": "node page.js",
    "robust": "node robust.js",
    "snap": "node export.js save",
    "snap:check": "node export.js check"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "jsdom": "^30.0.1"
  }
}
